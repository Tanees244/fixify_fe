import { Provider } from '@angular/core';

/**
 * Core singleton providers.
 * Import in app.config.ts if you need to provide them explicitly.
 * Most core services use providedIn: 'root', so this is optional.
 */
export const CORE_PROVIDERS: Provider[] = [];
